{
  "name": "nuxt-app",
  "private": true,
  "type": "module",
  "scripts": {
    "run": "nuxt dev  --dotenv .env --host -o",
    "build": "nuxt build  --dotenv .env",
    "clean": "nuxt cleanup",
    "preview": "nuxt preview",
    "generate": "nuxt generate --dotenv .env",
    "update": "nuxt upgrade -f",
    "prepare": "nuxt prepare",
    "analyze": "nuxt build --dotenv .env --analyze",
    "pnpm:i": "pnpm i"
  },
  "dependencies": {
    "@element-plus/nuxt": "^1.0.9",
    "vue": "3.4.34"
  },
  "devDependencies": {
    "@nuxtjs/tailwindcss": "^6.12.1",
    "nuxt": "^3.12.4",
    "sass": "^1.77.8"
  }
}
